export enum TagsEnum {
  PRODUCTIVITY = 'productivity',
  EDUCATION = 'education',
  HEALTH = 'health',
  URGENT = 'urgent',
}

export enum RouterEnum {
  MY_TASKS = 'myTodos',
  IMPORTANT = 'important',
  COMPLETED = 'completed',
  DELETED = 'deleted',
}

export enum RouterPathsEnum {
  MY_TASKS = '/',
  IMPORTANT = '/important',
  COMPLETED = '/completed',
  DELETED = '/deleted',
}
